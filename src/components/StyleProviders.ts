export const inputStyle = (w?: string): string =>
  `w-${
    w ?? 'full'
  } shadow-sm sm:text-sm rounded-md bg-neutral-900 focus:border-neutral-500 focus:outline-none focus:ring-1 focus:ring-neutral-500 block border-neutral-600`
